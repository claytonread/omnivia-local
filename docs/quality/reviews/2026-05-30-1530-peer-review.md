# OmniVia Peer Review

## Summary

Code review of the last 5 commits covering the comment-pass improvements, domain model ADR, review workflow documentation, and ingestion service implementation. The code is well-commented, architecturally sound, and follows OmniVia patterns.

## Files Reviewed

| File | Lines | Review Focus |
|------|-------|--------------|
| `services/api/database.py` | 61 | Core models and initialization |
| `services/api/main.py` | 659 | FastAPI handlers, Node/Edge/Source endpoints |
| `services/api/models.py` | 215 | SQLAlchemy models and Pydantic schemas |
| `services/api/services/embedding.py` | 45 | FastEmbed integration |
| `services/api/services/ingestion.py` | 209 | Vault scanning logic |
| `services/api/services/memory_service.py` | 49 | Business logic for memories |
| `services/api/services/vector_store.py` | 81 | Qdrant integration |
| `services/api/tests/test_ingestion.py` | 144 | Ingestion tests |
| `services/mcp_server/server.py` | 238 | MCP protocol implementation |
| `docs/adr/ADR-004-domain-model.md` | 150 | Domain model decision record |
| `ops/OPERATING_MODEL.md` | 202 | Operating model documentation |
| `ops/REVIEW_WORKFLOW.md` | 215 | Review workflow documentation |

## Overall Decision

**Approved**

## Findings

| Severity | Area | File | Issue | Recommendation |
|---|---|---|---|---|
| ~~Medium~~ | ~~API Design~~ | ~~main.py~~ | ~~`import uuid` and `import json` appear inline in multiple endpoint handlers~~ | **FIXED** - Committed in dc57f79 |
| ~~Low~~ | ~~Error Handling~~ | ~~ingestion.py~~ | ~~`read_file_content` returns raw bytes decoded without raising errors~~ | Not prioritized for MVP |
| Low | API Design | main.py | CORS is set to `allow_origins=["*"]` which is permissive for development. | Add TODO comment to restrict origins before production. |
| Note | Code Style | main.py | Section comments (`# ====`) use 50-char width which differs from Python PEP-8 convention but matches the file's existing style. | No change needed; consistency with existing style is fine. |

## Comment Quality Review

**Excellent.** The comment-pass improvements are highly effective:

- Module-level docstrings explain architectural layering (`main.py`)
- Domain model docstrings explain business rules (approval lifecycle in `database.py`, `memory_service.py`)
- Approval status lifecycle is clearly documented with plain-English explanations
- API handlers have docstrings explaining what each endpoint does
- Inline comments explain non-obvious logic (e.g., `limit * 2` for filtering compensation)

The commenting follows the OmniVia standard: explains *why*, not just *what*.

## Test Review

**Good coverage for ingestion service.** 18 tests covering:
- File type mapping (7 parametrized cases)
- Skip pattern logic (5 test cases)
- Workspace scanning integration (6 test cases)

**Missing but acceptable for MVP:**
- Tests for Node/Edge CRUD endpoints
- Tests for MemoryService methods
- Integration tests for the full API

The ingestion tests demonstrate good patterns (temp directories, parametrization) that can be extended.

## Security and Agent Safety Review

**Good.** Key safety features present:

- `approval_status` lifecycle prevents AI from silently creating trusted knowledge
- Symlink following disabled by default in workspace scanning
- Max files cap prevents runaway scans
- JSON metadata fields use proper serialization

**Minor concerns:**
- CORS `allow_origins=["*"]` is permissive (expected for dev, needs production hardening)
- No input validation on `node_type` field (accepts any string)

## Spec Alignment Review

**Fully aligned.** Code implements documented behavior:

- Memory model matches ADR-004 Phase 1
- Node/Edge models match ADR-004 Phase 2 design
- Source model provides provenance as specified
- Approval lifecycle: proposed → observed → approved matches documented behavior
- Ingestion skips cache directories and hidden files as expected

## External Code Boundary Review

**Clean.** No imports from `external/reference/` found in implementation code.

## Recommended Fix Order

1. ~~**Low priority**: Move repeated `import uuid` and `import json` to module level in `main.py`~~ - **FIXED** (dc57f79)
2. ~~**Low priority**: Add CORS production TODO comment~~ - **FIXED** (6605d1f)
3. ~~**Future**: Add Node/Edge and MemoryService tests~~ - **FIXED** (6605d1f: 17 new tests)

## Final Notes

The code quality is high. Comments are informative without being verbose. The architecture follows clear layering (handlers → services → database/vector). The approval status lifecycle is well-implemented and well-documented.

The 5 commits reviewed represent a solid foundation for the OmniVia knowledge graph with good separation between embedding-based (Memory) and structured (Node/Edge) knowledge paths.